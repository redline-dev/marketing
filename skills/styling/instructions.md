When creating styles use css modules
When creating css styles make sure to utilize appropirate theme variables as seen fit in `../../app/globals.css` if they don't exist create variables but try to reason with existing ones first
